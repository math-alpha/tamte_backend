const BASE_ERROR_DIR = "logs/errors"
const BASE_ACCESS_DIR = "logs/access"
const BASE_JOBS_DIR = "logs/jobs"
const BASE_ACTIONS_DIR = "logs/actions"

const LOG_HEADER = "Log file for ";

module.exports = {
    BASE_ACCESS_DIR,
    BASE_ACTIONS_DIR,
    BASE_JOBS_DIR,
    BASE_ERROR_DIR,
    LOG_HEADER
}